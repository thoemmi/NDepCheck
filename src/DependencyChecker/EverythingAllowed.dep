// (c) HMMüller 2006

  // A dependency that allows everything to use everything.
  // DO NOT USE THIS PRODUCTIVELY - it will render the
  // DependencyChecker useless!
** ---> **

  // Show all leaf namespaces in graph
// % (**).*
% (Microsoft.Practices.**).*

  // For System and Microsoft, show only top namespace
% (System.*).**
% (Microsoft.*).**
